<view wx:if = "{{!actionList.length}}" class="margin-tb-sm padding-top-lg padding-bottom-xl">
  <view class="flex justify-center text-xsl">
    <view class="cuIcon-searchlist text-gray icon"></view>
  </view>
  <view class="flex justify-center padding-top-sm">
    <view class="text-gray text">暂无数据</view>
  </view>
</view>

<block wx:for="{{actionList}}" wx:key="index">
	<view class="cu-card article {{isCard?'no-card':''}} margin" bindtap="toDetail" data-id="{{item._id}}">
		<view class="cu-item shadow">
			<view class="title">
				<view class="text-cut">{{item.actName}}</view>
			</view>
			<view class="content">
				<view class="desc">
					<view class="character">
						
						<view class="cu-tag bg-green light sm round" style="white-space:normal;">{{item.tag}}</view>
						<view class="cu-tag bg-green light sm round going-on" wx:if="{{item.status==1}}">进行中</view>
						<view class="cu-tag bg-green light sm round going-on" wx:elif="{{item.status==2}}" style="background-color: gray;">已结束</view>
						<view class="cu-tag bg-green light sm round going-on" wx:elif="{{item.status==0}}" style="background-color: gray;">审核中</view>
						<view class="cu-tag bg-green light sm round going-on" wx:elif="{{item.status==-1}}" style="background-color: gray;">拒绝发布</view>
						<view>
							<text class="data">{{data_Arr[index]}}</text>
						</view>
					</view>
				</view>
			</view>
		</view>
	</view>
</block>